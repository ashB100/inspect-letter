import { HighlightLetterDirective } from './highlight-letter.directive';

describe('HighlightLetterDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightLetterDirective();
    expect(directive).toBeTruthy();
  });
});
